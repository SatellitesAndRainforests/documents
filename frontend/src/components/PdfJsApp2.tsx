/*

import React, { useEffect } from 'react';


const PdfJsApp2: ( { fileUrl }: { fileUrl: string } ) => {

	useEffect( () => {

		const viewerUrl = `/web/viewer.html?file=${encodeURIComponent(fileUrl)}`;
		window.location.href = viewerUrl;

	}, [fileUrl] );

	return ( 

		<div>
			loading . . . 
		</div>

 	);

};

export default PdfJsApp2;


*/
